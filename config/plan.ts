const PlanConfig = {
  A: { name: "Option A", amount: 2500, interval: "monthly", accountLimit: 100 },
  B: {
    name: "Option B",
    amount: 10000,
    interval: "monthly",
    accountLimit: 500,
  },
  C: {
    name: "Option C",
    amount: 70000,
    interval: "one-time",
    accountLimit: 1000,
  },
};

export default PlanConfig;
